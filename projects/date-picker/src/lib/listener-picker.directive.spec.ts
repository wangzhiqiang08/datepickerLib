import { ElementRef } from '@angular/core';
import { ClickOutsideDirective } from './listener-picker.directive';

describe('ListenerPickerDirective', () => {
  it('should create an instance', () => {
  });
});
